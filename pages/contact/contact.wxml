<!--pages/contact/contact.wxml-->
<view class="container">
  <view class="body">
    <view wx:for="{{colorList}}" wx:key="index" class="numItem" style="background-color: rgba({{item}});">{{item}}</view>
  </view>
</view>